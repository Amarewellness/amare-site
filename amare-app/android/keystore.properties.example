# Copy to keystore.properties (gitignored) or run:
#   node scripts/write-android-keystore-properties.mjs
#
# storeFile is relative to amare-app/android/
storeFile=release/amare-release.jks
storePassword=YOUR_STORE_PASSWORD
keyAlias=amare
keyPassword=YOUR_KEY_PASSWORD
